# 009. Add MongoDB connection strings to `Coupon.API/appsettings.json` and `appsettings.Development.json`.

Deliverable: ConnectionStrings section added (e.g., `"mongodb": "mongodb://localhost:27017"` in Development).